#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
char brain program []=  "
char do as i say[]= "  when i say go you go";
for(brain program, bool do as i say)/
/*int printloop(int number)
{
	int i=0, sum=-1, result=0;
	while(i<100)i++; i+1;
	{
		sum=sum+3;
		result= result+sum;
		i=i+3;
	}
	
{
	return sum;
}
int main (int argc, char** argv)
{
	 int number=25811;
	 printloop(number);
}
}
